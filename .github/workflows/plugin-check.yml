name: 'WordPress Plugin Checks'
on:
  push:
    branches:
    - trunk
    - develop

  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Checkout
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

    - name: Run plugin check
      uses: wordpress/plugin-check-action@10857da14b6c2246d15402b3e69f777edcf8c12e # v1.1.9
      with:
        # Keep results in annotations and artifacts.
        repo-token: ''
        # Enforce warnings; ignore only reviewed codes below.
        ignore-warnings: false
        strict: true

        exclude-directories: |
          .github
          node_modules
          vendor
          tests
          bin
          build
          assets
          tools

        # Development configuration and documentation.
        exclude-files: |
          .git
          .gitignore
          .gitattributes
          .nvmrc
          .wp-env.json
          .stylelintrc.json
          .prettierrc.js
          .coderabbit.yaml
          phpcs.xml.dist
          phpcs-compat.xml.dist
          phpstan.neon.dist
          phpstan-baseline.neon
          AGENTS.md
          CLAUDE.md
          DEVELOPER.md

        # Existing metadata, WooCommerce contracts and reviewed legacy debt.
        # These codes also suppress future occurrences; review before extending.
        ignore-codes: |
          WordPress.DB.DirectDatabaseQuery.DirectQuery
          WordPress.DB.DirectDatabaseQuery.NoCaching
          WordPress.DateTime.RestrictedFunctions.date_date
          WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedClassFound
          WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedConstantFound
          WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedHooknameFound
          WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedVariableFound
          WordPress.Security.ValidatedSanitizedInput.InputNotSanitized
          plugin_header_nonexistent_domain_path
          trademarked_term
