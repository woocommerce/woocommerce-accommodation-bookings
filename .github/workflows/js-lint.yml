name: JavaScript Lint

on:
  workflow_call:
    inputs:
      frontend:
        description: Run the JavaScript and package checks.
        type: boolean
        default: true
      docs:
        description: Run the Markdown check.
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: '.nvmrc'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check authored JavaScript
        if: ${{ inputs.frontend }}
        run: npm run lint:js

      - name: Check dependency licenses
        if: ${{ !cancelled() && inputs.frontend }}
        run: npm run check-licenses

      - name: Check package metadata
        if: ${{ !cancelled() && inputs.frontend }}
        run: npm run lint:pkg-json

      - name: Check Markdown documentation
        if: ${{ !cancelled() && inputs.docs }}
        run: npm run lint:md:docs
